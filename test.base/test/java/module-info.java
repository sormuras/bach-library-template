open /*test*/ module test.base {
  // Modules under test
  requires com.github.sormuras.library;

  // Modules used for declaring and running tests
  requires org.junit.jupiter;
  requires static org.junit.platform.console;
}
